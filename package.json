{
  "name": "twilio-serverless-sms-group-chat",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "twilio-run deploy --force",
    "dev": "twilio-run --env --live --ngrok",
    "start": "twilio-run --env",
    "setup": "./scripts/setup.sh"
  },
  "devDependencies": {
    "twilio-run": "^2.9.0"
  },
  "engines": {
    "node": "8.10.0"
  },
  "dependencies": {}
}
